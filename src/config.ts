// Legacy entry point kept for backwards compatibility.
// All config now lives in src/config/index.ts to avoid duplicate definitions.
export { config } from './config/index';
export type { Config, SubscriptionTier } from './config/index';
